# Quality Rubric

- Use `discover_projects` when the path is unknown.
- Use `analyze_project` once a candidate path is known.
- Distinguish confirmed facts from inferred facts.
- Include stack, start command, test command, risks, quick wins, and next actions.
