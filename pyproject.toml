[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mimi-nox"
version = "4.0.0"
description = "◑ MiMi Nox – Offline-first local AI assistant. Private. Local. Yours."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [
    { name = "MiMi Tech AI UG", email = "hello@mimiai.de" },
]
keywords = ["ai", "agent", "ollama", "local", "privacy", "llm", "pwa", "artifacts", "computer-use", "vision", "scheduler", "mimi-nox", "schwarzwald"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Terminals",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "ollama>=0.3.0",
    "ddgs>=2.0.0",
    "chromadb>=0.6.0",
    "fastapi>=0.111.0",
    "uvicorn>=0.30.0",
    "python-multipart>=0.0.9",
    "pillow>=10.0.0",
    "qrcode[pil]>=7.4.2",
    "playwright>=1.40.0",
    "apscheduler>=3.10.0",
    "edge-tts>=7.0.0",
    "pdfplumber>=0.11.0",
    "reportlab>=4.0.0",
    "matplotlib>=3.8.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-benchmark>=4.0",
    "httpx>=0.27.0",
]
bench = [
    "pytest-benchmark>=4.0",
    "httpx>=0.27.0",
]
gui = [
    "pyautogui>=0.9.54",
    "mss>=9.0.1",
    "pynput>=1.7.6",
]
voice = [
    "faster-whisper>=1.0.0",
]
tui = [
    "textual>=0.70.0",
]

[project.scripts]
mimi-nox = "miminox:main"
miminox = "miminox_cli:main"
# keep clawdash alias for backwards compatibility / open-source users
clawdash  = "miminox:main"

[project.urls]
Homepage   = "https://github.com/bemlerlabs/mimi-nox"
Repository = "https://github.com/bemlerlabs/mimi-nox"
Issues     = "https://github.com/bemlerlabs/mimi-nox/issues"

[tool.hatch.build.targets.wheel]
packages = ["core", "ui", "server"]
include = ["miminox.py", "miminox_cli.py", "app/src/**"]

[tool.hatch.build.targets.wheel.shared-data]
"skills" = "share/mimi-nox/skills"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
